let sentence = 'i am good boy';
let reverse = '';
for (let letter of sentence) {
    // console.log(letter);
    reverse = letter + reverse;
}
console.log(reverse);
// we can reverse a sentence with this method.

const bakko = 'he is good in math';
for (i = 0; i < bakko.length; i++) {
    console.log(i);
}