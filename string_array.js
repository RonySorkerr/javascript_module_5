let numbers = [1, 5, 6, 45, 78, 9];
console.log(numbers);
numbers[0] = 12365; // we can change a value through selecting the index of an array.
console.log(numbers);

const name = 'MD.Rony Sorker';
console.log(name);
console.log(name.length);
console.log(name[3]); // we can find the index of an string with this method.

// NOTES
/**
 * STRING IS UNMUTABLE 
 * ARRAY IS MUTABLE 
 * IT MEANS YOU CAN CHANGE AN ARRAY BUT NOT A STRING.
 */